// The library's authored token file, so the docs show the values exactly as written there.
import tokensCss from '../../../../packages/react-input-calendar/src/styles/tokens.css?raw';
import { parseTokens } from './tokens';

export const tokenDefaults = parseTokens(tokensCss);
