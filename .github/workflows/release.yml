name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      # npm provenance (publishConfig.provenance) signs with the workflow's OIDC token.
      id-token: write
    env:
      # `secrets` is not allowed in an `if:` expression, so the check goes through env.
      HAS_NPM_TOKEN: ${{ secrets.NPM_TOKEN != '' }}
    # Actions pinned to release commits: this job can push, sign and publish. Dependabot bumps them.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org
      - run: pnpm install --frozen-lockfile
      # Built before the publish step, so the npm token is never in the build's environment.
      - run: pnpm build

      - name: Version PR, or publish
        if: env.HAS_NPM_TOKEN == 'true'
        uses: changesets/action@ae32849d5ba541f9ae29e40e22a623bc13562f51 # v2.1.2
        with:
          publish-script: pnpm exec changeset publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Without a token it only opens or updates the "Version Packages" PR, never publishes.
      - name: Version PR only
        if: env.HAS_NPM_TOKEN != 'true'
        uses: changesets/action@ae32849d5ba541f9ae29e40e22a623bc13562f51 # v2.1.2
